import { Field, ObjectType } from '@nestjs/graphql';

import { CommonVO } from '@/common/vo/common.vo';
import { createGQLResultsVO, createGQLResultVO } from '@/common/vo/result.vo';
import { CourseVO } from '@/modules/course/vo/course.vo';

import { {{pascalCase name}}Type } from '../models/{{kebabCase name}}.entity';

@ObjectType()
export class {{pascalCase name}}VO  extends CommonVO  {
  @Field({
    description: '消费卡名称',
  })
  name: string;

  @Field({
    description: '消费卡类型 ',
    nullable: true,
  })
  type: {{pascalCase name}}Type;

  @Field({
    description: '上课次数',
  })
  time: number;

  @Field({
    description: '有效期（天）',
  })
  validateDay: number;

  @Field(() => CourseVO, {
    description: '课程',
  })
  course: CourseVO;
}

@ObjectType()
export class {{pascalCase name}}ResultVO extends createGQLResultVO({{pascalCase name}}VO) {}

@ObjectType()
export class {{pascalCase name}}ResultsVO extends createGQLResultsVO({{pascalCase name}}VO) {}
